[project]
name = "amazon-java"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.57.0",
    "ruff>=0.14.10",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]
